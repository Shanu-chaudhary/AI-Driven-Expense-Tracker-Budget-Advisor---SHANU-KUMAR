spring.application.name=backend
spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/mydb}
server.port=${SERVER_PORT:8080}

# JWT and expiration — set via environment variables or application-secret.properties
jwt.secret=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIxMjM0NTY3ODkwIiwibmFtZSI6IkpvaG4gRG9lIiwiYWRtaW4iOnRydWUsImlhdCI6MTUxNjIzOTAyMn0.KMUFsIDTnFmyG3nMiGM6H9FNFUROf3wh7SmqJp-QV30
jwt.expiration=3600000

# ======================
# Email Configuration (do NOT commit credentials)
# Provide credentials via environment variables or an application-secret.properties file
# Example env names: MAIL_USERNAME, MAIL_PASSWORD
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:beingshanu789@gmail.com}
spring.mail.password=${MAIL_PASSWORD:rwed jftb yyax acqq}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# If you want to use an extra properties file for secrets, create
# backend/src/main/resources/application-secret.properties (gitignored)